# Enari Engine — map / environment record

## Observed map structure

Baked pool-day GLB world; render mesh is converted to physics collision in its sample.

## Highline application

Use map findings as design principles only: player routes need explicit anchors, physical transitions, recoveries and spatial context. No reference map, geometry, texture or place name is imported.
