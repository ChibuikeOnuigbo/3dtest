{
  "name": "the-last-signal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A compact first-person Three.js storm-relay mystery.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "qa": "npm run build && node qa/validate.mjs",
    "test:playthrough": "playwright test"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "vite": "^7.1.7"
  }
}
